/** @type {import('@remix-run/dev').AppConfig} */
export default {
  //esta parte es para iggnorar los archivos que no necesitemos
  ignoredRouteFiles: ["**/.*"],
  // appDirectory: "app",
  // assetsBuildDirectory: "public/build",
  // publicPath: "/build/",
  // serverBuildPath: "build/index.js",
};
